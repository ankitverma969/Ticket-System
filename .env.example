# Server Configuration
PORT=8080

# Database Configuration (for future stages)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/ticket_system?sslmode=disable

# JWT Configuration (for future stages)
JWT_SECRET=supersecretjwtkey_change_in_production
JWT_EXPIRATION=24h
